package com.javieralvarez;





import java.util.InputMismatchException;

import com.javieralvarez.clases.Conditions;

import junit.framework.Assert;
import junit.framework.TestCase;

public class ConditionsTest extends TestCase {
}

	
/*	public void testGetDateToString()  {
		
		Assert.assertEquals(Conditions.getDateToString(1), "13/05/2017");

	}*/
	
	/*
	public void testSetGet(){
		Conditions cond = new Conditions();
		cond.setChill(10);
		cond.setDayDescription("Lindo");
		cond.setHumidity(20);
		cond.setPressure(30);
		cond.setSunrise("40");
		cond.setSunset("50");
		cond.setTemp(60);
		cond.setVisibility(70);
		cond.setWindSpeed(80);
		
		Assert.assertEquals("Lindo", cond.getDayDescription());
		Assert.assertEquals((float)20, cond.getHumidity());
		Assert.assertEquals((float)30, cond.getPressure());
		Assert.assertEquals("40", cond.getSunrise());
		Assert.assertEquals("50", cond.getSunset());
		Assert.assertEquals((float)60, cond.getTemp());
		Assert.assertEquals((float)70, cond.getVisibility());
		Assert.assertEquals((float)80, cond.getWindSpeed());
			
		
	}
	
	public void testInput(){
		try{
		Conditions cond = new Conditions();
		cond.setCurrentConditions();
		}catch(InputMismatchException IME){
		Assert.fail();
		}
	}
	
	
}*/

